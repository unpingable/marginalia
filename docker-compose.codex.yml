# Optional Codex CLI provider. Only credential-isolated providerd receives auth.
services:
  marginalia-providerd:
    volumes:
      - marginalia_codex_home:/var/lib/marginalia/provider-home/.codex

volumes:
  marginalia_codex_home:
