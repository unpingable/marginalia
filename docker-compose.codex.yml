# Optional Codex CLI provider. AG invokes the mounted provider binary.
services:
  marginalia:
    environment:
      - BACKEND_TYPE=codex
      - CODEX_PATH=/app/codex-provider.sh
      - CODEX_NATIVE_PATH=/opt/codex/codex
    volumes:
      - marginalia_codex_home:/root/.codex

volumes:
  marginalia_codex_home:
