# Gate 1 production acceptance — 2026-09-07

Gate 1 is accepted as an independently shippable reliability and context baseline.
Platform migration and later semantic expansion remain separately gated.

## Candidate

- Git commit: `2ecae3950eb2b2676d00f8f4784303b7e0637a5c`
- Image tag: `marginalia:gate1-2ecae39`
- Image ID: `sha256:9d12961471608d8308bea1aeff8a80678dd3b910c37f2cd232f094a9393bd4dd`
- Image build time: `2026-09-07T04:45:30Z`
- Agent Governor contract commit: `e279a94326a0a13dbe43473846b53e4c3a9b31f2`

The candidate commit was pushed before image construction. The deployed application,
backup worker, and synthetic worker all resolve to the image ID above.

## Qualification

- Ruff formatting and lint checks passed.
- Python package wheel and source distribution built successfully.
- Full test suite: 801 passed.
- Browser JavaScript syntax and Compose configuration passed validation.
- Headless Chromium loaded both isolated and production UIs.
- The Story Bible exposes category completeness directly when adding a world rule,
  and possible-canon world rules expose the conservative “Category remains open”
  control.
- An unlisted cross-origin browser request received no CORS allow-origin header.
- A real governed Orion request passed in both the isolated candidate and production.

## Timeout repair

The production Orion provider now uses a 900-second total deadline, 600-second
read-idle deadline, and 10-second connection deadline. Outer bounds are ordered as:

- provider command: 960 seconds
- governor invocation: 980 seconds
- governor chat: 1000 seconds
- synthetic client: 1020 seconds
- wedge/capacity uncertainty threshold: 920 seconds

Client wait expiry and idle expiry are reported as unresolved execution, not proof
that a provider stopped or that retry is safe.

## State and rollback

The pre-deployment remote backup is
`marginalia-erin-20260907T044741124138Z.zip`, SHA-256
`29eec62ba4a7907c27896e5ba799d56c0fe5c5b2fbc869c1a1ec5ab78249377b`.
It was verified and restore-tested: 2 projects, 7 sessions, 747 messages, 2
artifacts, 133 canon reviews, and no untracked session files.

The exact previous production image
`sha256:cff36d032488377a3350d3dace66c43ef3cd1db336a4b25f6b59a4347e766acc`
successfully read candidate-written disposable state and wrote a new canon record.
The candidate then read that record and allocated the next monotonic canon ID.
The candidate stores new authority metadata in an additive sidecar ignored by the
previous image. Pre-release `.env` and provider-catalog copies remain under
`/opt/marginalia-local` with suffix `.pre-gate1-20260907T044741Z`.

## Production acceptance

After deployment, readiness reported healthy runtime and schema preflight,
maintenance inactive, zero in-flight executions, context preparation ready, the
remote NFS backup destination usable, and the original 2 projects, 7 sessions,
747 messages, and 20 checked state files present. A production Orion synthetic
returned PASS with an authority receipt. No release-related errors appeared in
the service logs.

Gate 1 is complete. Gate 2 is limited to read-only contract investigation; Gate 3
implementation requires explicit authorization.
